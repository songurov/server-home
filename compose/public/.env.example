# Domains
GITLAB_DOMAIN=gitlab.example.com
IMMICH_DOMAIN=immich.example.com
PLANE_DOMAIN=plane.example.com

# GitLab
GITLAB_HTTP_PORT=8088
GITLAB_SSH_PORT=2222

# Immich
IMMICH_VERSION=release
IMMICH_UPLOAD_LOCATION=./volumes/immich/library
DB_DATABASE_NAME=immich
DB_USERNAME=immich
DB_PASSWORD=change-this-immich-db-password

# Plane
# Plane se instaleaza separat cu installerul oficial in compose/plane
# si ruleaza local pe 3000 pentru proxy prin Caddy.
